public static class RunData
{
    // holds the XP to show on GameOver
    public static int FinalXP = 0;

    // call this to zero everything out (e.g. when you go back to Menu)
    public static void Reset()
    {
        FinalXP = 0;
    }
}